"use client";

import Spinner from "@/components/UI/SpinnerLoading";

const loading = () => {
  return (
    <div className="pt-[600px]">
      <Spinner />
    </div>
  );
};

export default loading;
