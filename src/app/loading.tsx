"use client";

import Spinner from "@/components/UI/SpinnerLoading";

const loading = () => {
  return (
    <div className="w-full h-full">
      <Spinner />
    </div>
  );
};

export default loading;
